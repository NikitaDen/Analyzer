let initialState: any = {};

const accountReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default accountReducer;