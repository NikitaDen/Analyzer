import React from "react";
import './account.scss';

const Account: React.FC = (props) => {
    return (
        <div className={'account'}>
            <h4>Account</h4>
        </div>
    )
};

export default Account;